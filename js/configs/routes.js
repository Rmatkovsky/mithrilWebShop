define( function( require ) {
    return function( iComponent ) {

        // m.route(document.body, "/", {
        //     "/": iComponent.login,
        //     "/login": iComponent.login,
        //     "/dashboard": iComponent.profile,
        // });
    };
});
